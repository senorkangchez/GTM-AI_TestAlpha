# Anthropic API key for the ONE live extraction call.
# Used by: scripts/precompute.ts, scripts/grade-rubric.ts, and app/api/extract/route.ts.
# Server-side only — never exposed to the client bundle.
# Copy this file to .env.local and paste your key. .env.local is gitignored.
ANTHROPIC_API_KEY=sk-ant-your-key-here
